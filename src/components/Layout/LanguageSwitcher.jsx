// Hidden until translations are complete (tabella `translations` ancora vuota).
// Per riattivare: ripristina il componente completo da git history.
export default function LanguageSwitcher() {
  return null
}
