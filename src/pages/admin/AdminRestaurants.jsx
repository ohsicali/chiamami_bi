import AdminDashboard from './AdminDashboard'

// STUB — aliases to AdminDashboard for now. Will be rewritten in STEP 4 (full restaurants list).
export default function AdminRestaurants() {
  return <AdminDashboard />
}
