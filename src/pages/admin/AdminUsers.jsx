import AdminLayout from '../../components/Layout/AdminLayout'

// STUB — will be rewritten in STEP 7 (full users list + stats)
export default function AdminUsers() {
  return (
    <AdminLayout title="Utenti">
      <div style={{ padding: '20px 28px' }}>
        <h1 style={{ fontSize: 20, fontWeight: 600, color: '#1a1a1f', margin: 0 }}>Utenti</h1>
        <p style={{ fontSize: 12, color: '#999', marginTop: 4 }}>
          Coming soon — in arrivo nel prossimo step.
        </p>
      </div>
    </AdminLayout>
  )
}
